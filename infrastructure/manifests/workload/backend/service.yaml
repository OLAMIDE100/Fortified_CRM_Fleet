apiVersion: v1
kind: Service
metadata:
  name: backend
  namespace: fortified-crm-fleet
  labels:
    app: backend
spec:
  type: ClusterIP
  ports:
    - name: http
      port: 8000
      targetPort: 8000
  selector:
    app: backend
